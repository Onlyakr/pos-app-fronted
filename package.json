{
  "name": "pos-system",
  "version": "1.0.0",
  "description": "Modern POS System with separate backend and frontend",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd pos-backend && npm run dev",
    "dev:frontend": "cd pos-frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd pos-backend && npm install",
    "build:frontend": "cd pos-frontend && npm run build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd pos-backend && npm start",
    "start:frontend": "cd pos-frontend && npm start",
    "install:all": "npm install && cd pos-backend && npm install && cd ../pos-frontend && npm install",
    "clean": "cd pos-backend && rm -rf node_modules && cd ../pos-frontend && rm -rf node_modules && rm -rf node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "workspaces": [
    "pos-backend",
    "pos-frontend"
  ]
}
